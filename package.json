{
  "name": "codegen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "NODE_ENV=dev node ./src/index.js create -p product -p person",
    "test": "NODE_OPTIONS=--experimental-vm-modules npx jest --watch --runInBand --no-chace --config",
    "test:cov": "NODE_OPTIONS=--experimental-vm-modules npx jest --config ./jest.config.mjs --coverage"
  },
  "keywords": [],
  "author": "jeandossantos",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.4.3"
  },
  "dependencies": {
    "chalk": "^5.2.0",
    "yargs": "^17.7.1"
  }
}
